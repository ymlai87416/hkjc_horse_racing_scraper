[pytest]
# pytest配置文件
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings
markers =
    integration: 集成测试标记
    unit: 单元测试标记
    slow: 运行较慢的测试

